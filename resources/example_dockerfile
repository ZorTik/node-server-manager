FROM openjdk:${JAVA_VERSION}

# ENV
# SERVICE_PORTS
# SERVICE_RAM
# STARTUP_FLAGS
# procesor???
# disk size???
# STARTUP_FILE

# port1 port2 port3
EXPOSE ${SERVICE_PORTS}

ENTRYPOINT [ "java", "-Xmx${SERVICE_RAM}M", "${STARTUP_FLAGS}", "-jar", "${STARTUP_FILE}", "--port=${SERVICE_PORT}" ]